-- --------------------------------------------------------
-- Database internal version 8: widen admin password column for password_hash() (bcrypt / argon2).
-- Backward compatible: existing MD5 hex strings remain valid until rehashed on login.
ALTER TABLE `stats_users` MODIFY `password` VARCHAR(255) NOT NULL DEFAULT '';
